/** @deprecated Import from `./theme/index` or `./theme` folder. Kept for existing imports. */
export {
  colors,
  type,
  space,
  radius,
  shadows,
  fontFamily,
  lightTheme,
  ThemeProvider,
  useTheme,
} from "./theme/index";
